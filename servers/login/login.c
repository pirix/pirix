#include <stdio.h>

int main(int argc, const char* argv[]) {
    puts("Hello World from Login!\n");

    while (1);

    //int fd = open("/etc/passwd", O_RDONLY);

    //printf("fd: %i\n", fd);

    while (1);

    return 0;
}
